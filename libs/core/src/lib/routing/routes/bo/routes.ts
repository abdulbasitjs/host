// TODO: need to test the and update the routes
const BO_ROOT_PATH = '';

export const BO_ROUTES = {
  ROOT: BO_ROOT_PATH,
  KYC_LVL2: `${BO_ROOT_PATH}/kyc-lvl2`,
  KYC_LVL3: `${BO_ROOT_PATH}/kyc-lvl3`,
} as const;
