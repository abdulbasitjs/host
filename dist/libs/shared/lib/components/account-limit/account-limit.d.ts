import { LimitDetails } from '../../typings';

interface LimitProps {
    limits: LimitDetails[];
    heading: string;
}
export declare function AccountLimit({ limits, heading }: LimitProps): import("react/jsx-runtime").JSX.Element;
export {};
